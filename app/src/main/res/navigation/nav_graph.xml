<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/fragment">

    <action
        android:id="@+id/action_open_dialog"
        app:destination="@id/dialog" />

    <fragment
        android:id="@+id/fragment"
        android:name="com.navigation.sample.MainFragment"
        android:label="DefaultFragment"
        tools:layout="@layout/fragment" />

    <dialog
        android:id="@+id/dialog"
        android:name="com.navigation.sample.MainDialog"
        tools:layout="@layout/dialog" />

</navigation>